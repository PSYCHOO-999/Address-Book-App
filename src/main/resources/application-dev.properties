# Limit logging to controller only (DEBUG for testing)
logging.level.com.bridgelabz.address_book.controller=DEBUG
logging.level.root=INFO

# Console log format
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - [%thread] - %-5level %logger{36} - %msg%n

# Log file (only for reference, won't be used for dev)
logging.file.name=logs/dev.log
